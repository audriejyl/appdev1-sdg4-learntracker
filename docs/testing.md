# Testing

This application uses Angular testing tools and Jasmine for unit tests.

## Run tests

```bash
npm test
```

## Notes

- The test suite runs with the Angular CLI and Karma.
- Use `npm test` to run tests once.
- Use `ng test` for watch mode during development.
